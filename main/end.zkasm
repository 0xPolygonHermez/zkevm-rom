finalWait:
    ${beforeLast()}                                       :JMPN(finalWait)
    ; Set registers to its initials values
    $ => SR                                               :RESTORE, JMP(start)